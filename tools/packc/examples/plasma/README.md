# Plasma Pack Examples

Plasma pack examples are data-only Pack v1 preview inputs.

The canonical product-owned example is:

`products/savers/plasma/content/v2/examples/plasma_lava_v2.toml`

This directory stays as a compiler-facing pointer so product content remains in
the Plasma subtree while pack compiler documentation can still route authors to
the supported example.
