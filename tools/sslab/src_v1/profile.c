#include "runtime_v1.h"

/*
 * ABI v1 profile orchestration is intentionally kept behind the existing
 * proof runner for C2. This translation unit reserves the ABI v1 profile
 * tranche without introducing arbitrary profile execution.
 */
