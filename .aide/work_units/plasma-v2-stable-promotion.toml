schema_version = 1
program_id = "plasma-v2-stable-promotion"
status = "active"
as_of = "2026-06-27"
source_commit = "c8b5ecf180f3b25adf7fdd2ca67dcd6693b90409"
source_mutation_by_aide = false
automatic_merge = false
automatic_release = false
automatic_promotion = false
claim_boundary = "PAW-I may decide stable promotion only through ScreenSave-owned gates; AIDE remains proposal, repair, and evidence coordination only."

[[work_units]]
id = "SS-PLV2-I0"
title = "Checkpoint Plasma v2 release candidate"
task_class = "stable-promotion"
objective = "Record Plasma v2 rc1 before stable-promotion review."
allowed_paths = ["validation/captures/plasma-v2/stable-promotion/intake-state.json", ".aide/work_units/plasma-v2-stable-promotion.toml", "docs/roadmap/paw-i-plasma-v2-stable-promotion.md"]
forbidden_paths = ["products/savers/plasma/src/**", "platform/**", "releases/**"]
selected_validation_tier = "T1"
expected_validators = ["py -3 tools/scripts/check_project_state.py --summary", "py -3 tools/scripts/check_plasma_v2_release_candidate.py", "git diff --check"]
evidence_outputs = ["validation/captures/plasma-v2/stable-promotion/intake-state.json"]
claim_boundary = "Checkpoint only; no stable promotion."
rollback_point = "c8b5ecf"
human_review_required = true
agent_allowed = true
agent_mode = "assistive"

[[work_units]]
id = "SS-PLV2-I1"
title = "Define Plasma v2 stable-promotion gate"
task_class = "stable-promotion"
objective = "Create the stable-promotion contract and validator."
allowed_paths = ["contracts/plasma_stable_promotion_v1.md", "products/savers/plasma/docs/plasma-v2-stable-promotion-policy.md", "tools/scripts/check_plasma_v2_stable_promotion_contract.py"]
forbidden_paths = ["PROJECT_STATE.toml", "releases/**", "products/savers/plasma/src/**"]
selected_validation_tier = "T1"
expected_validators = ["py -3 tools/scripts/check_plasma_v2_stable_promotion_contract.py", "py -3 tools/scripts/check_contracts.py"]
evidence_outputs = ["contracts/plasma_stable_promotion_v1.md"]
claim_boundary = "Contract only; no publication."
rollback_point = "c8b5ecf"
human_review_required = true
agent_allowed = true
agent_mode = "assistive"

[[work_units]]
id = "SS-PLV2-I2"
title = "Stage Plasma v2 stable-promotion artifacts"
task_class = "stable-promotion"
objective = "Create stable-promotion package staging with checksums, provenance, SBOM, and notes."
allowed_paths = ["packaging/windows/plasma-v2-stable-promotion/**", "tools/scripts/check_plasma_v2_package_stage.py"]
forbidden_paths = ["releases/**", "products/savers/plasma/src/**"]
selected_validation_tier = "T3"
expected_validators = ["py -3 tools/scripts/check_plasma_v2_package_stage.py", "py -3 tools/project_adapter/screensave_project.py validate --tier T3"]
evidence_outputs = ["packaging/windows/plasma-v2-stable-promotion/manifest.toml"]
claim_boundary = "Stable-promotion staging only; publication remains separate."
rollback_point = "c8b5ecf"
human_review_required = true
agent_allowed = true
agent_mode = "assistive"

[[work_units]]
id = "SS-PLV2-I3"
title = "Finalize Plasma v2 stable-promotion proof bundle"
task_class = "stable-promotion"
objective = "Record the stable-promotion proof matrix and bundle while preserving claim separation."
allowed_paths = ["validation/captures/plasma-v2/stable-promotion/proof-*", "validation/captures/plasma-v2/stable-promotion/evidence-index.json", "tools/scripts/check_plasma_v2_stable_promotion_evidence.py"]
forbidden_paths = ["releases/**", "products/savers/plasma/src/**"]
selected_validation_tier = "T2"
expected_validators = ["py -3 tools/scripts/check_plasma_v2_stable_promotion_evidence.py", "py -3 tools/scripts/check_proof_bundle_v1.py", "py -3 tools/scripts/check_aide_evidence_bridge.py", "py -3 tools/scripts/check_aide_evidence_index.py"]
evidence_outputs = ["validation/captures/plasma-v2/stable-promotion/proof-bundle-v1.json"]
claim_boundary = "Proof bundle only; proof pass is not publication or compatibility certification."
rollback_point = "c8b5ecf"
human_review_required = true
agent_allowed = true
agent_mode = "assistive"

[[work_units]]
id = "SS-PLV2-I4"
title = "Record final stable artistic acceptance"
task_class = "review"
objective = "Record final stable artistic acceptance for the bounded Plasma v2 scope."
allowed_paths = ["validation/captures/plasma-v2/final-artistic-decision/**", "tools/scripts/check_plasma_v2_final_artistic_acceptance.py"]
forbidden_paths = ["PROJECT_STATE.toml", "releases/**"]
selected_validation_tier = "T1"
expected_validators = ["py -3 tools/scripts/check_plasma_v2_final_artistic_acceptance.py"]
evidence_outputs = ["validation/captures/plasma-v2/final-artistic-decision/decision.stable.toml"]
claim_boundary = "Final artistic acceptance for named Plasma v2 scope only."
rollback_point = "c8b5ecf"
human_review_required = true
agent_allowed = true
agent_mode = "proposal-only"

[[work_units]]
id = "SS-PLV2-I5"
title = "Finalize Plasma v2 stable support claims"
task_class = "review"
objective = "Record evidence-classed stable support claims and explicit unsupported lanes."
allowed_paths = ["docs/compatibility/plasma-v2-stable.md", "products/savers/plasma/docs/plasma-v2-stable-support.md", "validation/captures/plasma-v2/stable-promotion/support-claims.json", "tools/scripts/check_plasma_v2_stable_support_claims.py"]
forbidden_paths = ["releases/**", "products/savers/plasma/src/**"]
selected_validation_tier = "T1"
expected_validators = ["py -3 tools/scripts/check_support_claims.py", "py -3 tools/scripts/check_plasma_v2_stable_support_claims.py"]
evidence_outputs = ["validation/captures/plasma-v2/stable-promotion/support-claims.json"]
claim_boundary = "Stable support wording only; broad OS certification remains unclaimed."
rollback_point = "c8b5ecf"
human_review_required = true
agent_allowed = true
agent_mode = "assistive"

[[work_units]]
id = "SS-PLV2-I6"
title = "Review stable provenance and security"
task_class = "review"
objective = "Record security, provenance, checksum, pack, dependency, license, and rollback checks."
allowed_paths = ["validation/captures/plasma-v2/stable-promotion/security-review.json", "validation/captures/plasma-v2/stable-promotion/provenance-review.json", "tools/scripts/check_release_provenance.py", "tools/scripts/check_release_security.py"]
forbidden_paths = ["products/savers/plasma/src/**", "platform/**", "releases/**"]
selected_validation_tier = "T2"
expected_validators = ["py -3 tools/scripts/check_release_provenance.py", "py -3 tools/scripts/check_release_security.py", "py -3 tools/scripts/check_packc.py"]
evidence_outputs = ["validation/captures/plasma-v2/stable-promotion/security-review.json", "validation/captures/plasma-v2/stable-promotion/provenance-review.json"]
claim_boundary = "Security/provenance review only; no publication."
rollback_point = "c8b5ecf"
human_review_required = true
agent_allowed = true
agent_mode = "assistive"

[[work_units]]
id = "SS-PLV2-I7"
title = "Record final Manager and Workbench inspection"
task_class = "review"
objective = "Record final stable-promotion Manager and Workbench inspection receipts."
allowed_paths = ["validation/captures/plasma-v2/stable-promotion/manager-final-review.json", "validation/captures/plasma-v2/stable-promotion/workbench-final-review.json", "tools/scripts/check_plasma_v2_stable_review.py"]
forbidden_paths = ["products/**", "platform/**", "releases/**"]
selected_validation_tier = "T2"
expected_validators = ["py -3 tools/scripts/check_manager_pack_preview.py", "py -3 tools/scripts/check_workbench_shell.py", "py -3 tools/scripts/check_plasma_v2_stable_review.py"]
evidence_outputs = ["validation/captures/plasma-v2/stable-promotion/manager-final-review.json"]
claim_boundary = "Inspection only; publication and compatibility certification remain separate."
rollback_point = "c8b5ecf"
human_review_required = true
agent_allowed = true
agent_mode = "assistive"

[[work_units]]
id = "SS-PLV2-I8"
title = "Burn down stable-promotion repair queue"
task_class = "repair"
objective = "Record repair queue and proposal-only burn-down for stable-promotion blockers."
allowed_paths = [".aide/repairs/**", "validation/captures/plasma-v2/stable-promotion/repair-burndown.json", "tools/scripts/check_repair_queue.py", "out/aide/agentic/SS-PLV2-I8/**"]
forbidden_paths = ["products/**", "platform/**", "releases/**"]
selected_validation_tier = "T1"
expected_validators = ["py -3 tools/scripts/check_repair_queue.py", "py -3 tools/scripts/check_agentic_policy.py", "py -3 tools/scripts/check_agentic_receipts.py"]
evidence_outputs = ["validation/captures/plasma-v2/stable-promotion/repair-burndown.json"]
claim_boundary = "Repair/proposal accounting only; no automatic mutation."
rollback_point = "c8b5ecf"
human_review_required = true
agent_allowed = true
agent_mode = "proposal-only"

[[work_units]]
id = "SS-PLV2-I9"
title = "Add Plasma v2 stable-promotion checker"
task_class = "stable-promotion"
objective = "Gate stable promotion before any state transition."
allowed_paths = ["tools/scripts/check_plasma_v2_stable_promotion.py", "validation/captures/plasma-v2/stable-promotion/gate-report.*"]
forbidden_paths = ["PROJECT_STATE.toml", "releases/**"]
selected_validation_tier = "T3"
expected_validators = ["py -3 tools/scripts/check_plasma_v2_stable_promotion.py", "py -3 tools/project_adapter/screensave_project.py validate --tier T2", "py -3 tools/project_adapter/screensave_project.py validate --tier T3"]
evidence_outputs = ["validation/captures/plasma-v2/stable-promotion/gate-report.json"]
claim_boundary = "Stable-promotion gate only; publication remains separate."
rollback_point = "c8b5ecf"
human_review_required = true
agent_allowed = true
agent_mode = "assistive"

[[work_units]]
id = "SS-PLV2-I10"
title = "Promote Plasma v2 to stable"
task_class = "stable-promotion"
objective = "Transition Plasma v2 to stable-promoted only after the stable-promotion checker passes."
allowed_paths = ["PROJECT_STATE.toml", "VERSION.toml", "README.md", "CHANGELOG.md", "docs/roadmap/**", "products/savers/plasma/docs/**", "tools/scripts/check_project_state.py", "tools/scripts/check_plasma_v2_stable_promotion.py"]
forbidden_paths = ["products/savers/plasma/src/**", "platform/**"]
selected_validation_tier = "T3"
expected_validators = ["py -3 tools/scripts/check_project_state.py --summary", "py -3 tools/scripts/check_plasma_v2_stable_promotion.py", "py -3 tools/scripts/check_docs_basics.py", "py -3 tools/scripts/check_contracts.py", "git diff --check"]
evidence_outputs = ["PROJECT_STATE.toml", "VERSION.toml", "docs/roadmap/plasma-v2-stable-promotion.md"]
claim_boundary = "Stable promotion state only; public publication remains separately gated."
rollback_point = "c8b5ecf"
human_review_required = true
agent_allowed = true
agent_mode = "assistive"
