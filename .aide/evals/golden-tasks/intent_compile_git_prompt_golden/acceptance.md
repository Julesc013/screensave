# Acceptance

- Prompt `merge dev to main` compiles to `git` with release/promotion risk.
- The packet blocks direct merge behavior.
- Validation hints require `git plan` and branch/promotion evidence.
