# Acceptance

- Changelog JSON contains source range, source head, commit counts, categories, entries, malformed commits, warnings, and preview-only flag.
- Release-note JSON contains highlights, validation summary, risk summary, follow-up, warnings, and preview-only flag.
- JSON output is deterministic and parseable.
