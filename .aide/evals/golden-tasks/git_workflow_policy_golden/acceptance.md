# Acceptance

- Git workflow policy exists with schema `aide.git-workflow-policy.v0`.
- `main` is canonical and `dev` is integration.
- `dev` is documented as shareable integration, not canonical truth.
- Q28 detection is report-only and forbids remote mutation.
- Project profiles include AIDE, Eureka, Dominium, website, and unknown repo defaults.
