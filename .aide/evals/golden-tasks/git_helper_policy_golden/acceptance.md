# Acceptance

- Helper policy exists with schema `aide.git-helper-policy.v0`.
- Helper commands are documented.
- Dry-run default is enforced.
- Protected branch roles are listed.
- Latest helper plan exists and records no remote mutation.
