# Acceptance

- Intent packet JSON contains required v0 fields.
- WorkUnit draft JSON contains required v0 fields and remains `draft`.
- Raw long prompt bodies are not stored; only hash and bounded excerpt are kept.
- Provider/model/network calls remain absent.
