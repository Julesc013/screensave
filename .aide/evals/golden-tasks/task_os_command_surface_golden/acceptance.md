# Acceptance

- `task status`, `task classify`, `task repair-plan`, `task requeue-plan`, and `task resume-plan` parse.
- `blocker status` and `blocker classify` parse.
- `wave status`, `wave plan`, `checkpoint status`, and `checkpoint plan` parse.
- Command registration stays local and report-only.
