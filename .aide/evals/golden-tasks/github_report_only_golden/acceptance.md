# Acceptance

- Advisory data keeps GitHub API mutation false.
- Advisory data keeps workflow installation and workflow writes false.
- Advisory data keeps branch mutation, tag creation, release publishing, network calls, and provider/model calls false.
