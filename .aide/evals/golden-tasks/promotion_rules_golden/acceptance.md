# Acceptance

- `task_to_dev` exists and requires clean or understood state, task evidence, validation, and commit checks.
- `dev_to_main` exists and requires review packet, full validation classification, changelog preview, and promotion evidence.
- Promotion rules remain policy-only in Q28.
- No merge, push, branch creation, or GitHub API mutation is implemented by this golden task.
