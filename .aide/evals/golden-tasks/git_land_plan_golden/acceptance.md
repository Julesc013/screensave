# Acceptance

- `git land` is documented.
- Land plans show `git merge --no-ff <source>`.
- `--apply` is documented as fixture/operator-controlled and not used on live AIDE in Q29.
- Latest helper plan records no remote mutation.
