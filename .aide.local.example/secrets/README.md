# Local Secrets Example

This directory documents where target-local secret placeholders may be
described without committing actual secret values.

Do not store provider keys, tokens, passwords, private keys, raw credentials,
or copied environment files in this example tree. Actual local secrets belong
outside the repository or under the ignored `.aide.local/` root.
