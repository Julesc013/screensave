# Local Ledgers Example

Future local runtime ledgers may live under `.aide.local/ledgers/` when a
reviewed phase enables them.

Committed AIDE ledger files under `.aide/reports/` remain estimated metadata
only. Local ledgers may contain machine-specific operational metadata, but must
not store raw prompts, raw responses, provider keys, or private user data by
default.
