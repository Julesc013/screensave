# Source Tree

The source tree is intentionally scaffolded during bootstrap before implementation exists.

- `src/common/` is for shared C89 utilities.
- `src/win32/` is for saver-host and Win32 integration code.
- `src/render/gdi/` is for the universal renderer floor.
- `src/render/gl11/` is for the optional conservative acceleration layer.
