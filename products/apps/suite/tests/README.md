# Suite Tests

This directory holds lightweight static or smoke-oriented validation for the `suite` app.

The initial `C09` smoke coverage is intentionally narrow:

- manifest-driven saver coverage stays explicit
- the suite app target can resolve the full current saver line plus `anthology`
- the app boundary remains separate from BenchLab and the standalone `.scr` products
