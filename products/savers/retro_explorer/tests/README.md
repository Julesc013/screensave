# Retro Explorer Smoke

This directory keeps lightweight product validation for Retro Explorer.
The current smoke check covers module validity, default config/clamp behavior, required preset/theme presence, and basic session progression through the shared saver contract.
