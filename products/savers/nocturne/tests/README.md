# Nocturne Tests

Purpose: lightweight product validation for the first real saver.
Belongs here: compile-friendly smoke checks for module wiring, defaults, preset presence, and theme presence.
Does not belong here: fake captures, fake screenshots, or heavyweight harness code.

Current stage:

- `smoke.c` checks the module descriptor, default config path, and curated preset/theme presence.

Type: product validation source.
