# Nocturne Source

Purpose: source files owned by the `nocturne` saver product.
Belongs here: product-local implementation once later series replace the Series 02 build stub.
Does not belong here: shared platform code or unrelated saver logic.
Current stage: the directory contains only a tiny build-only stub used to make the initial build graph concrete.
Type: product runtime source.
