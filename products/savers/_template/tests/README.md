# Template Saver Tests

Purpose: lightweight validation for the contributor starter saver.
Belongs here: compile-friendly smoke checks for module wiring, default config, and starter preset/theme presence.
Does not belong here: generated captures, shipping validation claims, or heavyweight harness code.

Current stage:

- `smoke.c` checks the module descriptor, default config path, and starter preset/theme coverage.

Type: product validation source.
