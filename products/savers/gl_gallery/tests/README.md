# GL Gallery Tests

This directory holds lightweight smoke validation for GL Gallery.
It keeps the product honest about identity, presets, themes, and config sizing without introducing a larger test harness.
