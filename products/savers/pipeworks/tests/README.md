# Pipeworks Smoke

This directory keeps lightweight product validation for Pipeworks.
The current smoke check covers module validity, default config/clamp behavior, required preset/theme presence, and basic session growth-state allocation through the shared saver contract.
