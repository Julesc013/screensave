# Atlas Smoke

This directory holds lightweight Atlas verification only.

The smoke target proves:

- the saver module metadata is valid
- default config, presets, and themes resolve cleanly
- a session can be created with deterministic seed input
- staged refinement allocates its bitmap state and survives a first step
