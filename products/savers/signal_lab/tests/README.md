# Signal Lab Smoke

This directory keeps lightweight product validation for Signal Lab.
The current smoke check covers module validity, default config and clamp behavior, required preset and theme presence, and a basic session tick through the shared saver contract.
