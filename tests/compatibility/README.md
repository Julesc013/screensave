# Compatibility Tests

Purpose: checks tied directly to the supported Windows band and compatibility contract.
Belongs here: tests and fixtures that exercise baseline assumptions and fallback rules.
Does not belong here: unsupported compatibility claims without evidence.
Type: verification.
