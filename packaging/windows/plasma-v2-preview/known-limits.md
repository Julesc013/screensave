# Plasma v2 Preview Known Limits

This package stage is for release-readiness review only.

Known limits:

- Plasma v2 remains `stable-candidate`, not stable.
- The GL11 realization is admitted as a candidate with portable proof fallback
  to the software reference.
- The staged `.scr` path is a build output reference, not a committed binary.
- Support class is `buildable`, not certified.
- Manager pack installation is not admitted.
- Release promotion remains blocked.
- compatibility certification is not claimed.
