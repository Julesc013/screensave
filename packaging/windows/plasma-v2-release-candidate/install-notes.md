# Plasma v2 Release-Candidate Install Notes

This staging directory is not an installer and is not a publication payload.

For operator testing, use the referenced `plasma.scr` artifact from the
VS2017 XP-lane output directory:

```text
out/msvc/vs2017_xp/Release/plasma/plasma.scr
```

Copying or installing the file remains a manual local test action. No Manager
install mutation is admitted by this package stage.

These notes do not make a compatibility certification claim.
