`PX11` is the transition-coverage and promotion-review tranche for post-`PL14` Plasma.

This directory is reserved for fresh `PX11` transition captures.
No new runtime captures were checked in from this checkout because no local compiler toolchain or prebuilt Plasma proof binaries were available.

Current `PX11` evidence therefore anchors to:

- `validation/captures/pl13/benchlab-plasma-journey-gl11.txt` for the checked-in lower-band transition report surface
- `products/savers/plasma/tests/px11-transition-proof.md` for the current bounded proof note
- `products/savers/plasma/tests/px11-promotion-review-proof.md` for the evidence inputs used in review 1

This is intentional.
`PX11` strengthens the transition matrix, fallback grammar, and reporting truth without fabricating fresh captures.
