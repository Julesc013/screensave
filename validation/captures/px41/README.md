# PX41 Capture Notes

`PX41` is a metadata-first and report-first tranche.

The truthful proof surface for this wave is:

- `python tools/scripts/plasma_lab.py integration-report`
- `python tools/scripts/plasma_lab.py control-report --profile <profile_key>`
- `python tools/scripts/plasma_lab.py curation-report`

No fresh rendered or compiled consumer captures are checked in here because this tranche does not claim live `suite`, `anthology`, or remote automation behavior in the current checkout.
