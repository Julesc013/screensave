# Truth And Proof Baseline Captures

This directory stores generated evidence for the truth-reset milestone.

The captures here are not new feature proof.
They exist to make current release and artifact truth inspectable before any
future support or compatibility claim is widened.

Current files:

- `pe-audit-current.txt`: PE architecture, subsystem, and import audit over the
  current checked-out release and build artifacts that are present under `out/`.
