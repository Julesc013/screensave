This directory is reserved for real `PX32` capture artifacts.

Current repo truth:

- `PX32` updates the bounded field-families-II and dimensional runtime subset
- no fresh rendered capture files were generated in this checkout
- the local compiler toolchain needed for a fresh compiled rerun was not available on `PATH`

Do not add placeholder captures here.
