# Plasma v2 Basic-Control Influence

Status: PAW-DX influence proof root.

`control-influence.json` records the deterministic Basic-control influence
classification for the Plasma v2 reference preview slice. Pixel-affecting
controls are rendered through the same Plasma v2 core used by the adapter.

The seed policy and quality intent entries are metadata-only in this slice:
the host applies seed selection policy, and the only admitted quality intent is
`safe`.
