# U09 Plasma Recut Captures

These files are the real generated artifacts used to support the final `U09`
recut.

They currently include:

- the current U08 QA-bar, surface-evaluation, and acceptance-decision reports
  rerun as living inputs to the final recut
- stable and experimental matrix or coverage reports from the current Lab shell
- stable metadata and control-profile reports
- the current thresholded preset-signature and theme-palette audit
- doc and SDK reference consistency checks used during the final curation pass

These captures are recut evidence, not new feature proof.
