# U08 Plasma QA Captures

These files are the real generated artifacts for the `U08` visual QA gate.

They currently include:

- the formal `qa-bar`, `surface-evaluation`, and `acceptance-decisions`
  reports from `plasma_lab.py`
- refreshed stable control-profile and curated-collection reports after the
  stable journey-key cleanup
- the current thresholded preset-signature and theme-palette audit used as one
  input to the acceptance review

These captures are evidence for the gate.
They do not replace the final `U09` recut.
