# Renderer Layer

Purpose: backend implementations behind the renderer contract.
Belongs here: GDI, GL11, explicit higher GL tiers, and the internal null safety fallback.
Does not belong here: product-specific scene logic or direct packaging concerns.
Type: runtime implementation.
